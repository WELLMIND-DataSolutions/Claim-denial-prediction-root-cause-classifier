services:
  - type: web
    name: claim-denial-api
    env: python
    plan: free
    buildCommand: pip install -r requirements.txt
    startCommand: uvicorn step12_Fastapi:app --host 0.0.0.0 --port $PORT
    envVars:
      - key: PYTHON_VERSION
        value: 3.11.9
